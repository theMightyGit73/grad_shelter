import React from 'react';

const Header = () => {
  return (
    // This <header> tag will be styled by the global CSS,
    // giving it a background color, padding, and center-aligned text.
    <header>
      <h1>The Graduate Shelter</h1>
      <p>Helping graduates find their first career, one step at a time.</p>
    </header>
  );
};

export default Header;